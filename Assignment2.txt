<?php
(Starts php)


(establishes connection with the database, localhost is IP, root and root are username/pass and demo is the name of the database currently working on)
$link = mysqli_connect("localhost", "root", "root", "demo");

 (If the connection fails it will echo the error response)
if($link === false){
    die("ERROR: Could not connect. " . mysqli_connect_error());
}

(inserts data into table called "persons" with the values listed)

$sql = "INSERT INTO persons (first_name, last_name, email_address) VALUES ('Wormy', 'VonWormenstein', 'email@email.com')";


(if the data was inserted correctly you will get the success message)
if(mysqli_query($link, $sql)){
    echo "Records added successfully.";

	
	(else you get the error message)
	} else{
    echo "ERROR: Could not able to execute $sql. " . mysqli_error($link);
}

(closes link)
mysqli_close($link);
(ends php)
?>
